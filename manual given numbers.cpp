#include<stdio.h>
main()
{
	 int a[5],i;
	 printf("\n Enter arrey elements = ");
	 
	 for(i=0;i<=4;i++)
	 {
	 	 scanf("\n %d",&a[i]);
	 }
	 printf("\n Display arrey elements = ");
	 
	 for(i=0;i<=4;i++)
	 {
	 	 printf("\n %d",a[i]);
	 }
}
